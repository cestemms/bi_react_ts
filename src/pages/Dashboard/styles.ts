import styled from 'styled-components';

export const Container = styled.div`
  padding: 1.5rem;
  margin-top: 2rem;
  margin-left: 1rem;  
  margin-right: 1rem; 
  
  border-radius: 1rem;
  height: calc(100vh - (var(--nav-size)*1.5));
  
  background: var(--card-background);
`;








